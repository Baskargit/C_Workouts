#include <stdio.h>
#include <stdlib.h>

struct Node
{
    int data;
    struct Node *next;
    struct Node *previous;
};

int main()
{
    struct Node *head = (struct Node *)malloc(sizeof(struct Node));

    for (int i = 0; i < 5; i++)
    {
        
    }
    


    return 0;
}
